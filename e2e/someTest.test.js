describe('Example', () => {
  beforeAll(async () => {
    // eslint-disable-next-line no-undef
    // await device.launchApp();
  });

  beforeEach(async () => {});

  it('dummy test example', async () => {});
});
